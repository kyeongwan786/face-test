[build.environment]
NODE_VERSION = "16"
NPM_FLAGS = "--legacy-peer-deps"

[build]
command = "bash build.sh"
publish = "build"

[functions]
node_bundler = "esbuild"

# ❌ 아래 줄 삭제하거나 주석 처리해야 정상 동작함
# [build.processing]
# skip_install = true
